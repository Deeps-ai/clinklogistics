// Lovable error reporting removed for production.
// This file intentionally left as a no-op module.
export function reportLovableError(_error: unknown, _context?: Record<string, unknown>) {
  // No-op: Lovable error reporting is not used in production.
}
